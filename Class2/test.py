def apply_discount(price, discount):
    discount_sum = price * (discount/100)
    return discount_sum

price=float(int("enter number: "))
   x=apply_discount(price, 10)
elif price >= 200:
    x=apply_discount(price, 20)
